{% extends 'base.html.twig' %}

{% block title %}ModaliteTransport{% endblock %}

{% block body %}
    <h1>ModaliteTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modalite_transport.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ modalite_transport.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modalite_transport_index') }}">back to list</a>

    <a href="{{ path('app_modalite_transport_edit', {'id': modalite_transport.id}) }}">edit</a>

    {{ include('modalite_transport/_delete_form.html.twig') }}
{% endblock %}
