<form method="post" action="{{ path('app_endroit_delete', {'id': endroit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ endroit.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
