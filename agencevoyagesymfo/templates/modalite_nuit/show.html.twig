{% extends 'base.html.twig' %}

{% block title %}ModaliteNuit{% endblock %}

{% block body %}
    <h1>ModaliteNuit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modalite_nuit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ modalite_nuit.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modalite_nuit_index') }}">back to list</a>

    <a href="{{ path('app_modalite_nuit_edit', {'id': modalite_nuit.id}) }}">edit</a>

    {{ include('modalite_nuit/_delete_form.html.twig') }}
{% endblock %}
